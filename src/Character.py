class Character: 

    def __init__(self, x, y, img_file):
        """ 
        initializes character object 
        Args:
            x (int): x coordinate  
            y (int): y coordinate  
            img_file (str): visual of character 
        """
        self = self.__init__
        x = self.x
        y = self.y 
        img_file = self.img_file
        
    def sprite(self, img_file):
        """
        alter the sprite image
        args: 
            img_file (str): different visual of character 
        """